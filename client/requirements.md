## Packages
framer-motion | Page transitions and stunning micro-interactions
date-fns | Human-readable date formatting and calculations
lucide-react | Beautiful SVG icons for the dashboard

## Notes
- Theme is a custom Dark Modern aesthetic designed for Plenitude Academia.
- Tailwind config relies on the CSS variables defined in index.css.
- The `api` object from `@shared/routes` is used strictly as defined in the API contract.
- Using `@assets/...` for the static logo import as requested.
